# Output schema
Outputs include runtime/profile/query, Presence decision/probability, cardinality, and a list of independently ranked instances. Each instance has a unique ID, candidate ID, parent-relative XYXY bbox, scores, uncompressed COCO RLE mask, and mask source.
