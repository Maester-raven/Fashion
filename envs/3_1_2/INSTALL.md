# Fashion 3.1.2 environment

`python scripts/download_models.py --module 3.1.2 --model-dir models --backend curl --resume --install` installs Release assets. Constrained subset is parsed from query text and filters semantic candidates by parent-coordinate geometry.
